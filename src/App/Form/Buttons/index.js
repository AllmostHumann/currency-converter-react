import { Button } from "./styled";

const Buttons = () => (
    <div>
      <Button
        type="submit"
      >
        Przelicz
      </Button>
      <Button
        type="reset"
      >
        Reset
      </Button>
    </div>
)

export default Buttons;